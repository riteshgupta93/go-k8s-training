---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: ecomapps.backend.ecom.app
spec:
  group: backend.ecom.app
  names:
    kind: EcomApp
    listKind: EcomAppList
    plural: ecomapps
    singular: ecomapp
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EcomApp is the Schema for the ecomapps API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of EcomApp
            properties:
              global:
                description: global vars
                properties:    
                  namespace:
                    description: Namespace where the app will be deployed
                    type: string     
                type: object    
              users:
                description: Different fields related to user service
                properties:
                  image:
                    description: image object
                    properties:     
                      repository:      
                        description: repository image used for user service
                        type: string
                      tag:
                        description: tag version of the image for user service
                        type: string
                    type: object    
                type: object
              orders:
                description: Different fields related to order service
                properties:
                  image:
                    description: image details object
                    properties:
                      repository:
                        description: repository image used for order service
                        type: string
                      tag:
                        description: tag version of the image for user service
                        type: string
                    type: object    
                type: object
            type: object
          status:
            description: Status defines the observed state of EcomApp
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
